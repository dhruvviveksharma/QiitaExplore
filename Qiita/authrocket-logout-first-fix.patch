diff --git a/qiita-control-plane/src/qiita_control_plane/auth/handoff.py b/qiita-control-plane/src/qiita_control_plane/auth/handoff.py
index 67e03da..9ba4968 100644
--- a/qiita-control-plane/src/qiita_control_plane/auth/handoff.py
+++ b/qiita-control-plane/src/qiita_control_plane/auth/handoff.py
@@ -171,11 +171,15 @@ def build_authrocket_login_url(
 ) -> str:
     """Build the AuthRocket LoginRocket Web login URL.
 
-    Appends `&prompt=login` to force interactive re-auth even when
-    AuthRocket has a cached browser session. We don't depend on this for
-    token freshness (AuthRocket re-emits the same JWT regardless), but it
-    blocks "logged-in browser walked away → attacker pivots" by requiring
-    a fresh password entry before issuing a new login.
+    Wraps the login URL in a `/logout?redirect_uri=` first. A cached
+    AuthRocket session must be cleared before the browser reaches
+    `/login` — otherwise the hosted "Need a login?" (/signup) link is
+    hijacked into "already signed in → complete the redirect", minting a
+    PAT for the wrong (previously cached) user. `prompt=login` alone
+    doesn't stop this: it only forces the login *form*, it does not clear
+    the session. `/logout` honors `redirect_uri`, and LoginRocket's SPA
+    preserves the inner login `redirect_uri` in sessionStorage, so the
+    handoff redirect still fires after a fresh login.
 
     `redirect_uri` is URL-encoded into the query string. The caller
     is responsible for ensuring it's an https:// URL pointing at qiita's
@@ -183,7 +187,8 @@ def build_authrocket_login_url(
     """
     base = loginrocket_base_url.rstrip("/")
     encoded_redirect = quote(redirect_uri, safe="")
-    return f"{base}/login?redirect_uri={encoded_redirect}&prompt=login"
+    login_url = f"{base}/login?redirect_uri={encoded_redirect}&prompt=login"
+    return f"{base}/logout?redirect_uri={quote(login_url, safe='')}"
 
 
 # ---------------------------------------------------------------------------
diff --git a/qiita-control-plane/src/qiita_control_plane/routes/auth.py b/qiita-control-plane/src/qiita_control_plane/routes/auth.py
index 1a94482..b7db5fa 100644
--- a/qiita-control-plane/src/qiita_control_plane/routes/auth.py
+++ b/qiita-control-plane/src/qiita_control_plane/routes/auth.py
@@ -387,7 +387,9 @@ async def begin_login(
 
     Sets a signed cookie carrying the timestamp (and CLI loopback port if
     `cli=1`), then 302s the browser to AuthRocket's hosted login UI with
-    `prompt=login` appended so AuthRocket forces interactive re-auth.
+    `prompt=login` appended so AuthRocket forces interactive re-auth. The
+    redirect now routes through LoginRocket `/logout` first, so a cached
+    AuthRocket session can't hijack the login/signup entry.
 
     The cookie is the freshness anchor for /auth/handoff — qiita doesn't
     rely on the JWT's `iat`/`auth_time` because LoginRocket Web re-emits
diff --git a/qiita-control-plane/tests/auth/test_handoff.py b/qiita-control-plane/tests/auth/test_handoff.py
index 2020b79..958ff8c 100644
--- a/qiita-control-plane/tests/auth/test_handoff.py
+++ b/qiita-control-plane/tests/auth/test_handoff.py
@@ -5,6 +5,7 @@ exercises the helpers in context against a real handoff flow.
 """
 
 import secrets
+from urllib.parse import parse_qs, urlparse
 
 import pytest
 from qiita_common.api_paths import URL_AUTH_HANDOFF
@@ -187,15 +188,20 @@ def test_hash_ot_code_matches_sha256_definition():
 # ---------------------------------------------------------------------------
 
 
-def test_build_authrocket_login_url_appends_prompt_login():
+def test_build_authrocket_login_url_routes_through_logout_and_forces_prompt_login():
     url = build_authrocket_login_url(
         loginrocket_base_url="https://realm.e2.loginrocket.com",
         redirect_uri=f"https://qiita.example.com{URL_AUTH_HANDOFF}",
     )
-    assert url.startswith("https://realm.e2.loginrocket.com/login?")
-    assert "prompt=login" in url
+    assert url.startswith("https://realm.e2.loginrocket.com/logout?redirect_uri=")
+    assert url.count("?") == 1  # the inner login `?` is encoded away
+
+    parsed = urlparse(url)
+    (login_url,) = parse_qs(parsed.query)["redirect_uri"]
+    assert login_url.startswith("https://realm.e2.loginrocket.com/login?")
+    assert "prompt=login" in login_url
     # redirect_uri must be URL-encoded — colon and slash both get %-escaped.
-    assert "redirect_uri=https%3A%2F%2Fqiita.example.com%2Fapi%2Fv1%2Fauth%2Fhandoff" in url
+    assert "redirect_uri=https%3A%2F%2Fqiita.example.com%2Fapi%2Fv1%2Fauth%2Fhandoff" in login_url
 
 
 def test_build_authrocket_login_url_strips_trailing_slash_on_base():
@@ -219,7 +225,10 @@ def test_build_authrocket_login_url_encodes_redirect_with_query_params():
         loginrocket_base_url="https://r.com",
         redirect_uri="https://qiita.example.com/handoff?env=dev",
     )
+    assert url.count("?") == 1  # only /logout's `?` — everything else is encoded
+
+    parsed = urlparse(url)
+    (login_url,) = parse_qs(parsed.query)["redirect_uri"]
     # `?env=dev` must be encoded into the redirect_uri value, not appended
     # as a separate query param to the AuthRocket URL.
-    assert "env%3Ddev" in url
-    assert url.count("?") == 1  # only AuthRocket's `?` — the inner one is encoded
+    assert "env%3Ddev" in login_url
